[build]
  command = "next build"
  publish = ".next"

[functions]
  directory = "pages/api"
  included_files = ["src/lib/**", ".env"]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
